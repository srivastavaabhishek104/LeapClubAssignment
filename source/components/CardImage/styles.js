import { StyleSheet } from "react-native"
import {normalize} from '../../utility/device';
const styles = StyleSheet.create({
    cardDataTextStyle:{
        fontWeight:"bold",
        fontSize:normalize(20),
    },
});

export default styles;